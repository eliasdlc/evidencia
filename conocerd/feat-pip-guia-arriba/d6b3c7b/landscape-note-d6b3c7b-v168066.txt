Landscape transition supplement, ConoceRD PiP native build d6b3c7b, versionCode 168066.
The active trip was rotated to 748x430 dp with adb shell wm user-rotation lock 1. The full-screen landscape capture shows the bottom-left trip column, Mapbox logo and attribution to its right, and the arrow in the map area.
Pressing Home entered PiP. Android's portrait-only launcher reset user rotation to portrait, so the same task reopened in portrait when launched from the launcher. After reopening, adb shell wm user-rotation lock 1 rotated the still-active trip back to landscape. The after-PiP landscape capture shows the column and ornaments in their normal landscape positions. This verifies landscape layout after a PiP cycle, but not a direct landscape-to-landscape PiP exit on this emulator.
